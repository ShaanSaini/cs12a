/* Sphere.c
 * Shaan Saini
 * shksaini
 * lab7
 * Returns the volume and surface area of a sphere with a given radius
 */

#include<stdio.h>
#include<math.h>

int main(){
  double x, volume, surfaceArea, r, rR;
  const double pi = 3.141592654;

  printf("Enter the radius of the sphere: ");
  scanf("%lf", &x);
  r = pow(x, 3);
  rR = pow(x, 2);
  volume = (4.0/3.0) * pi*r;
  surfaceArea = 4*pi*rR;
  
  printf("The volume is %lf and the surface area is %lf.\n", volume, surfaceArea);

  return 0;
}
